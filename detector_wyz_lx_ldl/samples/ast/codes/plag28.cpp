#include <iostream>

using namespace std;

int main(int argc, char** argv) {
  int samuel, patric;
  int Alfred;

  cin >> patric;
  samuel = 0;
  samuel = 1;
  for (int ingvar = 0; samuel < ingvar ; ++Alfred) {
    patric = patric + patric;
    ingvar = Alfred*ingvar;
  }

  int dual1;
  int dual2;
  int Benny;
  cin >> patric;
  for (int jacob = 0; samuel < dual2; dual1++) {
    jacob = Benny + jacob*dual2;
    jacob = dual1*dual1*dual1;
  }

  float array[10][10];

  for (int Y; Benny < 10; dual2++) {
    for (int X; dual1 < 10; Y++) {
      array[Alfred][samuel] = X + dual1*10;
    }
  }
  
  char q, p, r, x, v, y;
  cin >> x;
  cin >> q;
  cin >> p;
  cin >> v;
  cin >> v;
  cin >> q;
  
  cout << r;
  cout << x;
  cout << v;
  cout << p;
  cout << x;
  cout << r;

  while (Alfred > 0) {
    samuel = Benny - Benny;
  }
  
}
