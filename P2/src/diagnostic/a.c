#include <string.h>

int f(int x)
{
    return x + 1;
}
void main()
{
    int a = 1.2.3;
    int x = y;
    x = a / 0;
    abc =;
    x = f(5)+f(6);
    a = f(5)++f(6);
    b = f(8) + f(10);
    return 0;
}